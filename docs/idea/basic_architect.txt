[System Role]You are an Expert PyTorch Developer specializing in Bioinformatics and Deep Learning. Your task is to implement a Multimodal Deep Learning framework named "AMPR-Lite" (Adaptive Multimodal Protein Representation) for hierarchical multi-label Protein Function Prediction. The code will be run on Google Colab (limited VRAM, single GPU).[Project Context & Data Assumptions]Assume all raw data has been pre-computed offline and saved as .npy files. We are dealing with $N$ proteins and $C$ GO terms (classes).X_seq: ESM-2 embeddings. Shape: (N, 1280)X_3di: ProstT5 embeddings. Shape: (N, 512)X_ppi: Node2Vec embeddings. Shape: (N, 512)Y: Binary ground truth matrix. Shape: (N, C)DAG_Matrix: A binary matrix defining the Gene Ontology Hierarchy. Shape: (C, C). DAG_Matrix[i, j] = 1 means class j is a parent of class i.[Task 1: Custom Dataset & DataLoader]Write a PyTorch Dataset that loads these 4 pre-computed NumPy arrays. Include a standard DataLoader setup.[Task 2: The AMPR-Lite Model Architecture (nn.Module)]Implement the AMPRLite model with the following flow:Input Projections:Project X_seq from 1280 to $d=512$ using a Linear + ReLU + LayerNorm block.Project X_3di and X_ppi from 512 to $d=512$ using similar blocks.Modality Dropout (Training Only):To simulate missing data, randomly zero out the projected X_3di tensor with $p=0.15$.Randomly zero out the projected X_ppi tensor with $p=0.25$.Do not drop X_seq as it is the core modality.Adaptive Fusion (Gating Network):Concatenate the three $d=512$ vectors into a single vector of size $1536$.Pass through a Gating Network: Linear(1536, 128) -> ReLU -> Linear(128, 3) -> Softmax(dim=1).This outputs 3 weights: $\alpha_{seq}, \alpha_{3di}, \alpha_{ppi}$.Calculate the fused representation: $Z = \alpha_{seq} X_{seq} + \alpha_{3di} X_{3di} + \alpha_{ppi} X_{ppi}$. Size of $Z$ is $512$.Classifier:Pass $Z$ through a Linear(512, C) to get the logits.[Task 3: Custom Loss Function (BCE + DAG Loss)]Write a custom nn.Module for the loss function: TotalLoss = BCEWithLogitsLoss + lambda * DAG_Loss.Calculate probs = torch.sigmoid(logits).DAG Loss Logic: For every child-parent pair defined in DAG_Matrix, if probs[child] > probs[parent], apply a penalty.Math: $\mathcal{L}_{DAG} = \frac{1}{N} \sum \max(0, P_{child} - P_{parent})^2$.Hint for vectorization: Use broadcasting or matrix multiplication with DAG_Matrix to compute this efficiently without for-loops.[Task 4: Training Loop]Write a clean PyTorch training loop (Train + Validation).Use AdamW optimizer, OneCycleLR scheduler.Print training/val loss and average $\alpha$ weights (to show which modality the model relies on).Include basic boilerplate for moving tensors to cuda.[Output Requirements]Provide the complete, runnable Python code in a single, well-commented block. Use PyTorch typing and best practices. No dummy placeholders for the architecture—implement the exact math described above.